# agenticAI

This repository implements a toy AgenticAI agent. It uses a
src/ package layout and thin scripts that import from the `agenticai` package.


A fully autonomous incident‑response agent that uses LLM planning, MCP tool execution, and semantic memory to diagnose and summarise infrastructure issues.